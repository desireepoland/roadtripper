<h1>All Trips</h1>
<table>
  <tbody>
    <% @trips.each do |trip| %>
      <tr>
        <td>◦ <%= link_to trip.name, trip_path(trip.id) %></td>
      </tr>
    <% end %>
    <tr>
      <td><%= link_to "+ Create A Trip", new_trip_path %></td>
    </tr>
  </tbody>
</table>
