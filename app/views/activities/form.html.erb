<h1><%= @header %></h1>
<%= form_for [@trip, @stop, @activity] do |f| %>
    <table>
      <tbody>
        <tr>
          <td><%= f.label :name, "Name:" %></td>
          <td><%= f.text_field :name %></td>
        </tr>
      </tbody>
    </table>
    <%= f.submit %>
<% end %>
